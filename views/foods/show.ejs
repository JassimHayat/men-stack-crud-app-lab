

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= food.name %></title>
</head>
<body>
  <h1><%= food.name %></h1>


  <% if(food.isReadyToEat){ %>
    <p>This Meal is Ready to Eat.</p>
  <% }else{ %>
    <p>Sorry,This Meal is NOT Ready to Eat!</p>
  <% } %>
  <form action="/foods/<%=food._id%>?_method=DELETE" method="POST">
    <button type="submit">Delete <%= food.name %></button>
  
  </form>
  <a href="/foods/<%= food._id %>/edit">Edit <%= food.name %></a>
  <a href="/foods">Go back to all Foods</a>
</body>
</html>
